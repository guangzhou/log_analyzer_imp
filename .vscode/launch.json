{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run p1_run_first_pass (with await-llm)",
      "type": "python",
      "request": "launch",
      "module": "bin.p1_run_first_pass",
      "args": [
        "--path", "test.gz",
        "--size-threshold", "15",
        "--micro-batch", "30",
        "--max-per-micro-batch", "30",
        "--await-llm",
        "--force-flush"
      ],
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}",
      "justMyCode": true
    },
    {
      "name": "Run p1_run_first_pass (without await-llm)",
      "type": "python",
      "request": "launch",
      "module": "bin.p1_run_first_pass",
      "args": [
        "--path", "test.gz",
        "--size-threshold", "15",
        "--micro-batch", "30",
        "--max-per-micro-batch", "30",
        "--force-flush"
      ],
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}",
      "justMyCode": true
    }
  ]
}