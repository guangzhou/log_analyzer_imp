first_pass:
  buffer:
    max_window: 1000
    micro_batch: 200
    diversity:
      jaccard_threshold: 0.7
  matcher:
    indexer:
      strategy: "double_buffer"
  committee:
    backend: "langgraph"
    config_path: "configs/agents.yaml"
    secrets_path: "configs/secrets.yaml"   # 新增：指定密钥文件路径
